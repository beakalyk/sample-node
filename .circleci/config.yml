version: 2.1

orbs:
  node: circleci/node:16.17.0  #orb version

jobs:
  test:
    docker:
      - image: cimg/node:16.17.0
        auth:
          username: beakalyk
          password: 6D&QNC9HXA5o # context / project UI env-var reference
   - checkout
      - run: |
          docker --version # print the version for logging
          