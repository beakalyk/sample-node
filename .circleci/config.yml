orbs:
  docker: circleci/docker@1.7.0
  jobs:
  example-job:
    docker:
      # replace with your preferred image
      - image: cimg/node:16.17.0
    steps:
      - docker/build:
          image: simple-node . 

workflows:
  example-workflow:
    jobs:
      - example-job