version: 2.1

orbs:
  node: circleci/node@x.y #orb version

jobs:
  test:
    docker:
      - image: cimg/node:16.17.0
        auth:
          username: beakalyk
          password: 6D&QNC9HXA5o # context / project UI env-var reference
    steps:
      - checkout
      - node/install-packages # Utilize commands in steps
	    - docker psdocker --version # print the version for logging
