orbs:
  docker: circleci/docker@1.7.0

jobs:
  example-job:
    docker:
      # replace with your preferred image
      - image: cimg/base:stable
    steps:
      - docker/build:
          tag: beakalyk/simple-node:latest 
          image: simple-node .  

workflows:
  example-workflow:
    jobs:
      - example-job